//import * as joint from 'jointjs'

//TODO: Code taken from the jointjs documentation
//Currently not used, should discuss whether needed at all

/*
export const InfoButton = joint.elementTools.Button.extend({
    name: 'info-button',
    options: {
        focusOpacity: 0.4,
        x: '100%', 
        y: '0%',
        offset: {
            x: 0,
            y: 0
        },
        action: function() {
            const isSelected = this.model.attr('selected')
            if(isSelected) {
                this.model.attr('selected', false)
            }
            else {
                this.model.attr('selected', true)
            }
        },
        markup: [{
            tagName: 'circle',
            selector: 'button',
            attributes: {
                'r': 7,
                'fill': '#001DFF',
                'cursor': 'pointer'
            }
        }, {
            tagName: 'path',
            selector: 'icon',
            attributes: {
                'd': 'M -2 4 2 4 M 0 3 0 0 M -2 -1 1 -1 M -1 -4 1 -4',
                'fill': 'none',
                'stroke': '#FFFFFF',
                'stroke-width': 2,
                'pointer-events': 'none'
            }
        }]
    }
})
*/